while (True):
    try:
        number = int(input('Number: '))
        break
    except ValueError:
        print("Invalid")
        continue

def digit_count(:)
    number = str(number)
    length = len(number)

print(length)
