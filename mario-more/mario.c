#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int height;
    do
    {
        height = get_int("What is the height? ");
    }
    while (height > 0 && height < 9);

    for (i = 0; i < height; i++){

        printf(i)
    }

}
