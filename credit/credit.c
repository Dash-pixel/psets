#include <cs50.h>
#include <stdio.h>

int main(void)
{
//American Express cards have 15 digits, MasterCard has 16 digits,
//and Visa has either 13 or 16 digits. These numbers are made up of regular digits like 0, 1, 2, and so on.

// all American Express cards start with 34 or 37,
// most MasterCard cards start with 51, 52, 53, 54, or 55, and all Visa cards start with 4.
    long credit_number = get_long("Number:");

    if
    {

    }
    if else
    {

    }
    if else
    {

    }



// превратить все числа числа в отдельные диджиты
        for ( i = 1; i < 5; i++)
        {
           summ_of_digits += credit_number % (10 * i);
        }

}

